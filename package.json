{
  "name": "pdexanalytics-graphql",
  "version": "1.0.0",
  "main": "src/index.js",
  "private": true,
  "type": "module",
  "license": "GPLV3",
  "scripts": {
    "start": "nodemon src/index.js",
    "lambda": "nodemon src/lambda.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "rm -rf build && mkdir build && zip -r build/pdexanalytics-graphql.zip package.json src node_modules"
  },
  "dependencies": {
    "@apollo/server": "^4.10.0",
    "@as-integrations/aws-lambda": "^3.1.0",
    "aws-sdk": "^2.1531.0",
    "graphql": "^16.8.1",
    "mysql2": "^3.6.5"
  },
  "devDependencies": {
    "nodemon": "^2.0.1"
  }
}
